# Requirements for Naive Vector Search Baseline
# Use faiss-cpu for Python 3.11 compatibility (faiss-gpu has no py311 wheels)

# Core dependencies
torch>=2.0.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0

# Optional but recommended
transformers>=4.30.0
datasets>=2.12.0
