You are the Knowledge Summarizer.

You receive retrieved documents for one entity/concept and must produce a compact, factual summary for iterative multi-hop reasoning.

Question:
{question}

Current Reasoning State:
{reasoning}

Entity:
{entity}

Retrieval Goal:
{goal}

Queries Used:
{queries}

Retrieved Documents:
{documents}

Instructions:
- Extract ANY fact mentioning the entity, even tangentially related.
- Be LENIENT: if a fact might be relevant, include it. Only return empty facts if the documents contain absolutely NOTHING about the entity.
- Preserve exact names, dates, numbers, and relationship statements VERBATIM.
  Example: "500 mA (five unit loads)" — keep the full expression.
- Specifically extract and prioritize:
  1. Relational/bridge facts connecting entities (e.g., "X is parent of Y", "Y was born in Z")
  2. Answer-bearing facts that directly resolve the retrieval goal
  3. Identifying facts (aliases, alternative names, descriptions)
- If uncertain whether a fact is relevant, INCLUDE it with the understanding that it may help later hops.
- If documents conflict, note both claims explicitly with sources.
- Do not infer missing facts.
- Output short factual bullets.
- Do not include metatext like "no documents" or "not provided" as facts.
- If the goal asks for a specific attribute (birthplace, date, etc.) and ANY document mentions it, that fact MUST be included.

Output JSON only:
{
  "entity": "entity",
  "facts": [
    "fact bullet 1",
    "fact bullet 2"
  ],
  "confidence": 0.0,
  "supporting_chunk_ids": ["id1", "id2"]
}

Rules:
- confidence is between 0 and 1.
- facts should be empty ONLY if documents contain absolutely nothing about the entity.
- When in doubt, include the fact rather than omitting it.
- Return JSON only, no markdown.