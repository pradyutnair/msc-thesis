#!/bin/bash
#SBATCH --job-name=marag_llama3_hotpotqa
#SBATCH --partition=gpu_h100
#SBATCH --time=24:00:00
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=16
#SBATCH --gpus=1
#SBATCH --mem=200G
#SBATCH --output=jobs/output/marag/hotpotqa_llama3_%j.log

module purge
module load 2023
module load Python/3.11.3-GCCcore-12.3.0
module load CUDA/12.1.1

source /projects/prjs1800/venvs/marag-py311-venv/bin/activate
export PYTHONNOUSERSITE=1
export HF_HOME=/projects/prjs1800/.cache/huggingface
export PYTHONUNBUFFERED=1
export VLLM_BASE_URL=http://localhost:8000/v1

cd /projects/prjs1800/external/MA-RAG
export PYTHONPATH=/projects/prjs1800/external/MA-RAG:$PYTHONPATH

# Create fresh results directory for llama3
RESULTS_DIR="plan_rag_extract_llama3-8B_hotpotqa"
mkdir -p $RESULTS_DIR

echo "=== Starting vLLM with Llama-3-8B ==="
nvidia-smi --query-gpu=name,memory.total --format=csv

python -m vllm.entrypoints.openai.api_server \
    --model meta-llama/Meta-Llama-3-8B-Instruct \
    --download-dir $HF_HOME \
    --port 8000 \
    --dtype bfloat16 \
    --max-model-len 8192 \
    --gpu-memory-utilization 0.95 \
    --max-num-seqs 64 \
    --trust-remote-code > /tmp/vllm.log 2>&1 &

VLLM_PID=$!

echo "Waiting for vLLM (up to 10 min)..."
for i in {1..120}; do
    curl -s http://localhost:8000/v1/models > /dev/null 2>&1 && { echo "Ready after $((i*5))s"; break; }
    [ $i -eq 120 ] && { echo "vLLM failed to start:"; cat /tmp/vllm.log; exit 1; }
    sleep 5
done

python main.py --model llama3-8B --dataset hotpotqa --exp plan_rag_extract --gpus 0 --start_index 0 --end_index 500

echo "=== Final Results ==="
echo "Total results: $(ls $RESULTS_DIR/*.json 2>/dev/null | wc -l)"
kill $VLLM_PID 2>/dev/null
