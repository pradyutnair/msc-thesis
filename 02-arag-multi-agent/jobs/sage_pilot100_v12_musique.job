#!/bin/bash
#SBATCH --job-name=s_p100_v12
#SBATCH --partition=gpu_h100
#SBATCH --time=02:30:00
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=16
#SBATCH --gpus=1
#SBATCH --mem=96G
#SBATCH --output=/projects/prjs1800/msc-thesis/02-arag-multi-agent/results/logs/%j_sage_pilot100_v12_musique.out

set -euo pipefail
module purge
module load 2025
module load Anaconda3/2025.06-1
module load CUDA/12.8.0

cd /projects/prjs1800/msc-thesis/02-arag-multi-agent
source /projects/prjs1800/venvs/arag-venv/bin/activate

export PYTHONNOUSERSITE=1
export HF_HOME="/projects/prjs1800/.cache/huggingface"
export HF_HUB_CACHE="$HF_HOME"
export TRANSFORMERS_CACHE="$HF_HOME"
export TOKENIZERS_PARALLELISM=false
export ARAG_API_KEY="dummy"
export ARAG_BASE_URL="http://127.0.0.1:8000/v1"
export ARAG_MODEL="Qwen3-30B-A3B"

MODEL_ID="Qwen/Qwen3-30B-A3B"
SERVED_NAME="Qwen3-30B-A3B"
QFILE="data/musique/questions_pilot100_seed42.json"
ROOT_OUT="results/sage_pilot100_seed42_v12"
OUT="$ROOT_OUT/v12"
mkdir -p "$ROOT_OUT" results/logs
rm -rf "$OUT"

cleanup() {
  [[ -n "${VLLM_PID:-}" ]] && kill -0 "$VLLM_PID" 2>/dev/null && kill "$VLLM_PID" || true
}
trap cleanup EXIT TERM INT

echo "=== MuSiQue pilot100 (seed42): v12 only ==="
echo "Questions file: $QFILE"
echo "Start: $(date)"

vllm serve "$MODEL_ID" \
  --served-model-name "$SERVED_NAME" \
  --host 127.0.0.1 --port 8000 \
  --dtype auto \
  --max-model-len 32768 \
  --gpu-memory-utilization 0.94 \
  --max-num-seqs 96 \
  --enable-prefix-caching \
  --enable-auto-tool-choice \
  --tool-call-parser hermes \
  --trust-remote-code \
  --generation-config vllm &
VLLM_PID=$!

for i in $(seq 1 360); do
  if ! kill -0 "$VLLM_PID" 2>/dev/null; then
    wait "$VLLM_PID" || true
    exit 1
  fi
  if curl -fsS http://127.0.0.1:8000/v1/models 2>/dev/null | grep -q "$SERVED_NAME"; then
    echo "vLLM ready after ~$((i*5))s"
    break
  fi
  sleep 5
done

PYTHONPATH=src python -u scripts/multi_agent_runner.py \
  --config configs/sage_v12_musique.yaml \
  --questions "$QFILE" \
  --output "$OUT" \
  --limit 100 \
  --concurrent 20

PYTHONPATH=src python -u scripts/eval_offline.py "$OUT/predictions.jsonl"

python - <<'PY'
import json
from pathlib import Path
from collections import Counter

root = Path('results/sage_pilot100_seed42_v12')
v12_path = root/'v12'/'predictions.jsonl'
v7_path = Path('results/sage_v7_1000/musique/predictions.jsonl')
e4_path = Path('/projects/prjs1800/msc-thesis/01-arag-reproduction/results/qwen3-30b-e5-deepseekr1/musique/predictions.jsonl')


def load_jsonl(p):
    out=[]
    with open(p) as f:
        for line in f:
            line=line.strip()
            if line:
                out.append(json.loads(line))
    return out


def norm(s):
    import re, string
    s=(s or '').lower()
    s=re.sub(r'\b(a|an|the)\b',' ',s)
    s=''.join(ch for ch in s if ch not in string.punctuation)
    return ' '.join(s.split())


def score(rows):
    n=len(rows)
    em=0
    by_hop=Counter()
    by_hop_n=Counter()
    for r in rows:
        pred=norm(r.get('pred_answer',''))
        gold=norm(r.get('gold_answer') or r.get('answer',''))
        ok=int(pred==gold and gold!='')
        em+=ok
        qid=r.get('qid') or r.get('id','')
        hop='unknown'
        if isinstance(qid,str):
            if '2hop' in qid: hop='2hop'
            elif '3hop' in qid: hop='3hop'
            elif '4hop' in qid: hop='4hop'
        by_hop[hop]+=ok
        by_hop_n[hop]+=1
    return {
        'n': n,
        'em_correct': em,
        'em': em/n if n else 0.0,
        'by_hop': {
            h: {'n': by_hop_n[h], 'em_correct': by_hop[h], 'em': (by_hop[h]/by_hop_n[h] if by_hop_n[h] else 0.0)}
            for h in sorted(by_hop_n)
        }
    }

v12 = load_jsonl(v12_path)
subset = {r.get('qid') or r.get('id') for r in v12}
all_v7 = load_jsonl(v7_path)
all_e4 = load_jsonl(e4_path)
v7 = [r for r in all_v7 if (r.get('qid') or r.get('id')) in subset]
e4 = [r for r in all_e4 if (r.get('qid') or r.get('id')) in subset]

s12 = score(v12)
s7 = score(v7)
se4 = score(e4)

summary = {
    'subset_size': len(subset),
    'v12': s12,
    'v7_same_subset': s7,
    'e4_same_subset': se4,
    'delta_v12_minus_v7_em_pp': round((s12['em'] - s7['em']) * 100, 2),
    'delta_v12_minus_e4_em_pp': round((s12['em'] - se4['em']) * 100, 2),
}

out_json = root/'pilot_v12_vs_baselines.json'
out_txt = root/'pilot_v12_vs_baselines.txt'
out_json.write_text(json.dumps(summary, indent=2))

lines = []
lines.append(f"n={summary['subset_size']}")
for name, s in [('v12', s12), ('v7', s7), ('e4', se4)]:
    lines.append(f"{name}: EM={s['em']*100:.2f}% ({s['em_correct']}/{s['n']})")
    for hop in ['2hop','3hop','4hop']:
        if hop in s['by_hop']:
            hs=s['by_hop'][hop]
            lines.append(f"  {hop}: {hs['em']*100:.2f}% ({hs['em_correct']}/{hs['n']})")
lines.append(f"delta v12-v7: {summary['delta_v12_minus_v7_em_pp']:+.2f} pp")
lines.append(f"delta v12-e4: {summary['delta_v12_minus_e4_em_pp']:+.2f} pp")
out_txt.write_text('\n'.join(lines)+"\n")
print('\n'.join(lines))
print(f"saved: {out_json}")
print(f"saved: {out_txt}")
PY

echo "=== Done === $(date)"
